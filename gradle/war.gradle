apply plugin: "war"

bootWar {
    mainClass = "xyz.onegrid.austin.AustinApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
